<UserControl
    x:Class="Jaya.Ui.Views.NavigationView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:Jaya.Ui.Converters;assembly=Jaya.Ui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:m="clr-namespace:Jaya.Ui.Models;assembly=Jaya.Ui"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="320"
    d:DesignWidth="220"
    BorderBrush="{DynamicResource ThemeControlMidBrush}"
    BorderThickness="1"
    DataContext="{Binding NavigationViewModel, Source={DynamicResource ViewModelLocator}, Mode=OneTime}"
    IsVisible="{Binding PaneConfig.IsNavigationPaneVisible, Mode=OneWay}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <c:BitmapValueConverter x:Key="BitmapValueConverter" />
    </UserControl.Resources>

    <TreeView
        BorderThickness="0"
        Items="{Binding Node.Children}"
        SelectedItem="{Binding SelectedNode, Mode=OneWayToSource}"
        SelectionMode="Single">
        <TreeView.Styles>
            <Style Selector="TreeViewItem">
                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=OneWayToSource}" />
            </Style>
        </TreeView.Styles>
        <TreeView.ItemTemplate>
            <TreeDataTemplate ItemsSource="{Binding Children}">
                <StackPanel Orientation="Horizontal">
                    <Image
                        Width="16"
                        Height="16"
                        Margin="0,0,4,0"
                        Source="{Binding IconImagePath, Converter={StaticResource BitmapValueConverter}}" />
                    <TextBlock Text="{Binding Label}" />
                </StackPanel>
            </TreeDataTemplate>
        </TreeView.ItemTemplate>
    </TreeView>
</UserControl>
